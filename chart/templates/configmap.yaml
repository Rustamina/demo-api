apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "demo-api.fullname" . }}-config      # имя из fullname + суффикс "-config"
  labels:
    {{- include "demo-api.labels" . | nindent 4 }}
data: 
  {{- toYaml .Values.config | nindent 2 }}
